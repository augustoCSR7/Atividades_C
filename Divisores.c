#include <stdio.h>
int main(){
	int i, num, quantidade, divisores, dividir;
	printf("Digite o numero: \n");
	scanf("%d", &num);
	
	for(i=1; i<=num; i++){
		dividir = 1;
		if(num%dividir==0){
			num == divisores;
			printf("%d \n", divisores);
		}
		else{
			break;
		}
		dividir++;
	}
	
}
